"use strict"//to use a newer version of java script//

//alert( 3 + 3) //it will not work, because we are using node js, not a browser,so it will not support it here,hence remove this code to execute all the rest.//

//let name="jojo"
//console.log(typeof "jojo")

console.log( 2 > 1);